#include <stdio.h>

int main()
{

    float r = 6, h = 10;
    printf("the radis is %f area %f\n", r, 3.14 * r * r);
    printf("the radis is %f and the hight is %f and cylinder volume is %f ", r, h, 3.14 * r * r * h);

    return 0;
}